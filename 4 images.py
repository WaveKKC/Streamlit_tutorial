# import  library
import streamlit as st 
import os
# in terminal => streamlit run  {fileName}.py 
# CRT + C => cancel run server

# Images

# st.image(os.path.join(os.getcwd(), "static", "image1.jpg"))
st.image(os.path.join(os.getcwd(), "static", "image1.jpg"), width=100)
